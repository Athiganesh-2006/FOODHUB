# Empty in dev: requests go to the Vite dev server's own origin and are proxied
# to the backend (see vite.config.js), which sidesteps CORS entirely.
# For a real deployment set the absolute backend URL, e.g.
#   VITE_API_URL=http://localhost:8087
VITE_API_URL=
